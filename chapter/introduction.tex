\chapter{序論}

宇宙マイクロ波背景放射（Cosmic Microwave Background; CMB）の温度異方性の精密測定により、ビッグバン宇宙模型は今日、宇宙の標準模型として確立した。しかし、この模型には「地平線問題」・「平坦性問題」・「残存粒子問題」などの未解決な問題が存在する。本章では、これらを解決する有力な理論として、インフレーション理論について述べ\cite{matsubara, Baumann:2009ds}、この理論を検証するCMB偏光観測とその現状についてまとめる。最後に、本研究で開発するフロントエンド回路のアプリケーションとなるGroundBIRD実験の概要について述べる。

\section{インフレーション理論と宇宙マイクロ波背景放射}

\subsection{ビッグバン宇宙模型の限界とインフレーション理論の導入}

インフレーション理論は、1981年に発表された、初期宇宙の進化を記述する理論である\cite{1981mnras.195..467S, PhysRevD.23.347}。この理論は、ビッグバン宇宙模型の問題であった「地平線問題」・「平坦性問題」・「残存粒子問題」を一挙に解決する。これら3つの問題は、宇宙初期がフリードマン的な宇宙であるために存在する。そこで、インフレーション理論では、宇宙初期のある一定期間をフリードマン的ではなく、ド・ジッター的であったと仮定する。このド・ジッター的な期間（インフレーション期）は、ハッブル・パラメーター$H:=\dot{a}/a$が一定となるため、$a\propto \mathrm{e}^{Ht}$となり、時空が指数関数的に引き伸ばされる。ここで、$a:=a(t)$は宇宙の膨張率を表し、スケール因子と呼び、$\dot{a}:=\mathrm{d}a(t)/\mathrm{d}t$である。この加速度的な急膨張をインフレーションと呼ぶ。

インフレーション期が存在すると、フリードマン的であるとして求めた地平線（粒子地平線）は実際の地平線ではなくなる。インフレーション期以前に因果関係を持っていた時空の2点は、インフレーションによって急激に引き離される。その共動距離が現在の見かけの地平線半径よりも大きければ、現在の宇宙が地平線よりも大きなスケールで因果関係を持っていたとしても問題にはならない。よって、地平線問題は解決する。

さらに、インフレーションは時空の曲率を急激に小さくする。インフレーション期が十分長ければ、曲率はゼロに漸近してゆき宇宙は平坦になる。つまり、平坦性問題も解決する。また、インフレーション期以前に生成された残存粒子の密度も、インフレーションによって急激に小さくなる。よって、残存粒子問題も解決する。

フリードマン的な宇宙における見かけの地平線を表す量として、ハッブル半径$d_{\mathrm{H}}:=c/H$を考える。%これは、ある時刻の膨張率で決まる時間スケールに
宇宙のある場所に固定された任意の2点を考え、その物理的距離を$l_{\mathrm{phys}}$とする。この距離はスケール因子$a$に比例し、これらの長さの時間変化は図\ref{fig:inflation_scale}のようになる。

\begin{figure}[htbp]
 \centering
 \includegraphics[width=11cm]{figures/inflation_scale.png}
 \caption[インフレーション理論におけるスケールの変化]{インフレーション理論におけるスケールの変化。太線は各期間のハッブル半径$d_{\mathrm{H}}$の変化を表す。細線は共動スケール$l_{\mathrm{phys}}$の変化を表す。下の細線は放射優勢期にハッブル半径に入る共動スケール、上の細線は現在のハッブル半径に入る共動スケールである。}
 \label{fig:inflation_scale}
\end{figure}

インフレーションの始まる時刻と終わる時刻をそれぞれ、$t_{\mathrm i}$、$t_{\mathrm f}$とし、それに対応するスケール因子を$a_{\mathrm i}=a(t_{\mathrm i})$、$a_{\mathrm f}=a(t_{\mathrm f})$とする。%インフレーションより以前$t<t_{\mathrm i}$がどのようになっていたかは模型にもよるが、

いま、インフレーションが起きる前には、$l_{\mathrm{phys}}$がハッブル半径の中に入っており、そのスケールで宇宙は一様になっているとする。インフレーション期$t_{\mathrm i}<t<t_{\mathrm f}$に入るとハッブル・パラメーターは一定値をとり、$d_{\mathrm H}=\mathrm{const.}$、$l_{\mathrm{phys}}\propto \mathrm{e}^{Ht}$となる。これにより、$l_{\mathrm{phys}}$は指数関数的に大きくなり、ハッブル半径を超える。インフレーションが終わった後$t>t_{\mathrm f}$では再び、放射優勢のフリードマン的な宇宙になり、ハッブル半径の増加率が$l_{\mathrm{phys}}$のそれを上回るようになる。こうして一度ハッブル半径の外へ出たスケールが再びハッブル半径内に戻る。つまり、現在の地平線内に入ってくるスケールも、インフレーション以前に因果関係を持つことが可能となる。

どのくらいの期間インフレーションが起きればよいか調べるために、インフレーション前後のスケール因子の比をとり、それを対数表示した量$N$を定義する。
\begin{equation}
 N := \ln\left[a(t_{\mathrm f}) \over a(t_{\mathrm i})\right]
    = \int_{t_{\mathrm i}}^{t_{\mathrm f}} H\mathrm{d}t
    \label{eq:expanding_index}
\end{equation}
ここで、$N$は膨張指数と呼ばれる。インフレーション中は、ハッブル・パラメーターが一定なので、
\begin{equation}
 N = H(t_{\mathrm f})(t_{\mathrm f} - t_{\mathrm i})
\end{equation}
となる。したがって、膨張指数はインフレーション中の膨張率とその持続時間に比例する。

\subsection{インフレーションの模型---スローロール・インフレーション}

以上より、インフレーション理論は、宇宙初期にド・ジッター的な宇宙が実現すると仮定している。ド・ジッター的な宇宙は、アインシュタイン方程式に宇宙定数を導入することにより実現するが、宇宙定数は語義どおり定数なので、インフレーションを始めたり、終わらせたりすることができない。

一般に、インフレーションの模型を考えるときは、なんらかのスカラー場などによって、一時的に現れた真空のエネルギーがド・ジッター的な宇宙を実現していると考える。そのようなスカラー場をインフラトン場と呼ぶ。

ここでは簡単のために、インフラトン場が実スカラー場$\phi(x)$である場合を考え、量子ゆらぎについて平均したインフラトン場の有効ポテンシャルを$V(\phi)$とする。作用積分のローレンツ不変性と一般座標変換不変性、運動方程式の時間に対する微分は2階までであるとすると、スカラー場のラグランジアン$L$が一意に定められる。
\begin{equation}
 L = \int \sqrt{-g}\, \mathrm{d}^{3}x \left[-\frac{1}{2}g^{\mu\nu} \partial_{\mu}\phi \partial_{\nu}\phi - V(\phi) \right]
\end{equation}
ここで、$L$は共動座標$x^{i}$による積分であり、$g=\det (g_{\mu\nu})$は計量テンソルの行列式である。

一様等方宇宙を考えると、スカラー場は時間のみの関数となり、$\sqrt{-g} \propto a^{3}$である。したがって、単位共動体積あたりのラグランジアン$\mathcal{L}$は
\begin{equation}
 \mathcal{L} = a^{3}\left[\frac{\dot{\phi}^{2}}{2c^{2}} - V(\phi)\right]
  \label{eq:lag_dens}
\end{equation}
となる。これより、オイラー--ラグランジュ方程式を求めると、
\begin{equation}
 \ddot{\phi} + 3H\dot{\phi} + c^{2}V'(\phi) = 0
  \label{eq:eular_eq}
\end{equation}
となる。ここで、$V'(\phi)=\mathrm{d}V(\phi)/\mathrm{d}\phi$である。この方程式は、摩擦を受けながら1次元ポテンシャルの中を運動する粒子の描像と等価である。つまり、インフレーションという急膨張を引き起こすエネルギーが摩擦熱に変換されることでインフレーションを終わらせる機構になっている。

式(\ref{eq:lag_dens})のラグランジアンをルジャンドル変換してハミルトニアンを求めれば、場のエネルギーを計算することができる。それを場の量で書きなおして、場のエネルギー密度$\rho_{\phi}$を求めると、
\begin{equation}
 \rho_{\phi} = \frac{\dot{\phi}^{2}}{2c^{2}} + V(\phi)
  \label{eq:energy_dens}
\end{equation}
となる。さらに、一様等方宇宙における保存則より、場の圧力$p_{\phi}$は
\begin{equation}
 p_{\phi} = \frac{\dot{\phi^{2}}}{2c^2} - V(\phi)
\end{equation}
となる。ここで、$\dot{\phi}=0$のとき、$V(\phi)$は一定となる。これは宇宙定数のエネルギー密度と圧力に一致する。つまり、インフレーション理論に必要なド・ジッター的な宇宙がこのスカラー場によって実現されている。

インフレーションを起こす例として、図\ref{fig:slow-roll}のようなポテンシャルを考える。このスカラー場は、インフレーションが始まるときに、なんらかの理由によって真の真空ではない$\phi=\phi_{\mathrm{in}}$という値にある。スカラー場の値はポテンシャルの最小点である真の真空$\phi_{0}$に向ってゆっくり落ちていき、インフレーションを起こす。そして、$\phi_{0}$のまわりを振動して急速にその真空のエネルギーを解放しインフレーションが終了する。

\begin{figure}[htbp]
 \centering
 \includegraphics[width=12cm]{figures/slow-roll.png}
 \hspace{-2cm}
 \caption[スローロール・インフレーション]{スローロール・インフレーション}
 \label{fig:slow-roll}
\end{figure}

このインフレーションの期間は、曲率を現在の宇宙のように小さくするために、十分長く起きる必要がある。そのため、ポテンシャルの形はある程度制限される。インフラトンの描像が摩擦を受けながらポテンシャル中をゆっくりと運動する粒子の運動と等価であることは先に述べた。この摩擦が十分大きければ、すぐに摩擦とポテンシャルがつりあい、ある一定の終端速度をもった状態に落ち着く。このときは、式(\ref{eq:eular_eq})における加速度の項$\ddot{\phi}$が無視できるため、式(\ref{eq:eular_eq})は、
\begin{equation}
 3H\dot{\phi} + c^{2}V'(\phi) = 0
  \label{eq:eular_eq'}
\end{equation}
となる。ここで、スカラー場の真空のエネルギーが卓越しているとすると、フリードマン方程式は
\begin{equation}
 H^{2} = \frac{8\pi G}{3c^{2}}V(\phi)
  \label{eq:friedmann_eq_slow-roll}
\end{equation}
と書ける。これらの近似は、スローロール条件と呼ばれ、その条件は以下である。
\begin{equation}
 \dot{\phi}^{2} \ll c^{2}V(\phi), \quad |\ddot{\phi}| \ll c^{2}|V'(\phi)|
  \label{eq:slow-roll}
\end{equation}
また、式(\ref{eq:eular_eq'})--(\ref{eq:slow-roll})により、ポテンシャルの微分に対する条件は、
\begin{equation}
 \left(V' \over V\right)^{2} \ll \frac{24\pi G}{c^{4}}, \quad \biggl| {V'' \over V} \biggr| \ll \frac{24\pi G}{c^{4}}
  \label{eq:slow-roll_diff}
\end{equation}
である。これより、式(\ref{eq:expanding_index})で表されるインフレーションの膨張指数$N$は、式(\ref{eq:eular_eq'})--(\ref{eq:slow-roll})より、
\begin{equation}
 N = \int_{t_{\mathrm i}}^{t_{\mathrm f}} H \mathrm{d}t
   = \int_{\phi_{\mathrm i}}^{\phi_{\mathrm f}} \frac{H}{\dot{\phi}}\mathrm{d}\phi
   = -\frac{8\pi G}{c^4}\int_{\phi_{\mathrm i}}^{\phi_{\mathrm f}}\frac{V(\phi)}{V'(\phi)}\mathrm{d}\phi
   \label{eq:expanding_index_slow-roll}
\end{equation}
となる。よって、式(\ref{eq:slow-roll_diff})、(\ref{eq:expanding_index_slow-roll})より、$|\phi_{\mathrm f}-\phi_{\mathrm i}|\sim c^{2}G^{-1/2}$より大きい値であれば、$N$は十分大きくなる。つまり、十分長いインフレーションは、このように比較的小さな傾きを持つポテンシャルによって起こる。

\subsection{宇宙論的摂動}

インフレーションのエネルギースケールを考えるために、一様等方宇宙からの摂動を考える。
\begin{equation}
 \phi(t, \bm{x}) = \bar{\phi}(t) + \delta\phi(t, \bm{x}), \quad g_{\mu\nu}(t, \bm{x}) = \bar{g}_{\mu\nu}(t) + \delta g_{\mu\nu}(t, \bm{x})
\end{equation}
ここで、$\bar{\phi}$と$\bar{g}_{\mu\nu}$は、背景場と一様等方な背景を表し、$\delta \phi(t, \bm{x})$と$\delta{g_{\mu\nu}}(t, \bm{x})$はその摂動を表す。%計量$\mathrm{d}s^{2}$は、
% \begin{equation}
% \begin{aligned}
%  \mathrm{d}s^{2} &= g_{\mu\nu}\mathrm{d}x^{\mu}\mathrm{d}x^{\nu} \\
%  &= -(1 + 2\Phi)\mathrm{d}t^{2} + 2a B_{i}\mathrm{d}x^{i}\mathrm{d}t + a^{2}[(1 - \Psi)\delta_{ij} + E_{ij})]\mathrm{d}x^{i}\mathrm{d}x^{j}
%  \end{aligned}
% \end{equation}
% となる。ここで、$B_{i}$、$E_{ij}$は、
% \begin{equation}
%  \begin{aligned}
%   B_{i}  := \partial_{i}B - S_{i}, \quad (\partial^{i}S_{i} &= 0), \\
%   E_{ij} := 2\partial_{ij}E + 2\partial_{(i}F_{j)} + h_{ij}, \quad (\partial^{i} F_{i} &= 0, \ h^{i}{}_{i} = \partial^{i}h_{ij} = 0)
%  \end{aligned}
% \end{equation}
%である。
実空間における、スカラー・ベクトル・テンソル摂動は、それぞれ分解できることが知られており、独立に考えられる。また、ベクトル摂動
%$S_{i}$、$F_{i}$
は、宇宙の膨張にしたがって減衰する成分であるとしてここでは扱わない。

\subsubsection{スカラー摂動}

スカラー摂動は、一様な超曲面上$\xi$の曲率摂動により与えられる\cite{PhysRevD.28.679}。
\begin{equation}
 -\xi := \Psi + \frac{H}{\dot{\bar{\rho}}}\delta \rho
  \label{eq:xi}
\end{equation}
式(\ref{eq:xi})は、スローロール・インフレーションの場合、
\begin{equation}
 -\xi \approx \Psi + \frac{H}{\bar{\dot{\phi}}}\delta \phi =: \mathcal{R}
  \label{eq:xi'}
\end{equation}
と近似できる。スカラーゆらぎのパワースペクトルは、
\begin{equation}
 \left<\mathcal{R}_{\bm{k}}\mathcal{R}_{\bm{k}'}\right>
  = (2\pi)^{3}\delta(\bm{k} + \bm{k'})P_{\mathcal{R}}(k),
  \quad \Delta_{\mathrm s}^{2}
  := \Delta_{\mathrm{\mathcal R}}^{2}
  = \frac{k^{3}}{2\pi^{2}}P_{\mathcal R}(k)
  \label{eq:ps}
\end{equation}
となる。ここで、$\left<\cdots\right>$は、ゆらぎのアンサンブル平均である。パワースペクトルのスケール依存性を考えるとき、次のような指標が用いられる。
\begin{equation}
 n_{\mathrm s} - 1 := \frac{\mathrm{d}\ln \Delta_{\mathrm{s}}^{2}}{\mathrm{d}\ln k}, \quad \alpha_{\mathrm s} := \frac{\mathrm{d}n_{\mathrm s}}{\mathrm{d}\ln k}
  \label{eq:n_s}
\end{equation}
ここで、スケール不変であるとき、$n_{\mathrm s}=1$である。
spatially-flatゲージを考えると、式(\ref{eq:xi'})において$\Psi=0$となり、$\mathcal{R}$とインフレーションのスカラー場の関係は、
\begin{equation}
 \left< \mathcal{R}_{\bm{k}} \mathcal{R}_{\bm{k'}} \right>
  = \left(H \over \dot{\phi}\right)^{2} \left< \delta \phi_{\bm{k}} \delta \phi_{\bm{k'}}\right>
  \label{eq:R_and_phi}
\end{equation}
となり、スローロール・インフレーションにおける、スカラー場（$m_{\phi}\ll H$）の量子ゆらぎは、ハッブル・パラメーターを使って、
\begin{equation}
 \left< \delta\phi_{\bm{k}} \delta\phi_{\bm{k'}} \right>
  = (2\pi)^{3}\delta(\bm{k} + \bm{k'})\frac{H^{2}}{2k^{3}}
  \label{eq:fluc_phi}
\end{equation}
となる。したがって、式(\ref{eq:ps})、(\ref{eq:R_and_phi})、(\ref{eq:fluc_phi})より、スカラー場がつくるゆらぎのパワースペクトルは次のようになる。
\begin{equation}
 \Delta_{\mathrm s}^{2}(k) = \left.\left(\frac{H}{2\pi}\right)^{2}{H^{2}\over 2k^{3}}\right|_{k=aH}
\end{equation}

\subsubsection{テンソル摂動}

テンソル摂動には2つのモード$h_{ij}$（$h := h^{+},\ h^{\times}$）が存在する。このテンソルゆらぎのパワースペクトルは、
\begin{equation}
 \left< h_{\bm{k}} h_{\bm{k'}} \right> = (2\pi)^{3}\delta(\bm{k} + \bm{k'})P_{h}(k)
  , \quad
  \Delta_{h}^{2} = \frac{k^{3}}{2\pi^{2}}P_{h}(k)
\end{equation}
となる。2つのモードを合わせたパワースペクトルは、
\begin{equation}
 \Delta_{\mathrm t}^{2} := 2\Delta_{h}^{2} = \left.\frac{2}{\pi^{2}}\left(H\over M_{\mathrm{pl}}\right)^{2}\right|_{k=aH}
  \label{eq:ps_tensor}
\end{equation}
で与えられる。スケール依存性の指標は、スカラー摂動のときと同様にして、次のように定義される。
\begin{equation}
 n_{\mathrm t} := \frac{\mathrm{d}\ln \Delta_{\mathrm t}^{2}}{\mathrm{d}\ln k}
\end{equation}

\subsection{インフレーションのエネルギー・スケール}

テンソルゆらぎの振幅$\Delta_{\mathrm t}^{2}$をスカラーゆらぎの振幅$\Delta_{\mathrm s}^{2}$で規格したパラメーターを定義する。
\begin{equation}
 r := {\Delta_{\mathrm t}^{2}(k) \over \Delta_{\mathrm s}^{2}(k)}
\end{equation}
ここで、$r$はテンサー・スカラー比と呼ばれる。$\Delta_{\mathrm s}^{2}$を固定して、$\Delta_{\mathrm t}^{2}\propto H^{2}\approx V$とすると、テンサー・スカラー比は、インフレーションのエネルギー・スケールを表す。
\begin{equation}
 V^{1/4} \sim \left(r \over 0.01\right)^{1/4} \times 10^{16} \quad \mathrm{[GeV]}
\end{equation}
これより、$r\geq 0.01$のとき、インフレーションのエネルギー・スケールは素粒子の大統一理論（GUT）のエネルギー・スケール（$\approx 10^{16}$ GeV）と一致する。

実験サイドで考えれば、$r$を精密に測定することにより、インフレーションのエネルギー・スケールに制限を加えることができる。つまり、理論家百家争鳴の時代に一石を投じることができる。%終止符を打つことができるのである。

% \subsection{宇宙マイクロ波背景放射}

\subsection{CMBの偏光}

CMBは電子とのトムソン散乱によって生じたものである。一般に、トムソン散乱は偏光を生じるため、CMB偏光も偏光している。一様等方な宇宙では、散乱によって生じる偏光は等方化されて消えてしまうが、宇宙が非一様的であれば、散乱は非等方的になり、観測されるCMBに偏光が残る（図\ref{fig:thomson_scattering}）。

\begin{figure}[htbp]
 \centering
 \includegraphics[width=9cm]{figures/thomson_scattering.png}
 \caption[非等方的なトムソン散乱]{非等方的なトムソン散乱により偏光がつくられる}
 \label{fig:thomson_scattering}
\end{figure}

CMBの偏光は、次の$E$モードと$B$モードに分解することができ、次のように定義する。
\begin{equation}
 \begin{pmatrix}
  E(\bm{l}) \\
  B(\bm{l})
 \end{pmatrix}
 :=
 \begin{pmatrix}
  \cos 2\phi_{l}  & \sin 2\phi_{l} \\
  -\sin 2\phi_{l} & \cos 2\phi_{l}
 \end{pmatrix}
 \begin{pmatrix}
  \widetilde{Q}(\bm{l}) \\
  \widetilde{U}(\bm{l})
 \end{pmatrix}
\end{equation}
ここで、$\widetilde{Q}$、$\widetilde{U}$は、ストークス・パラメーターの2次元フーリ変換であり、第1軸と2次元波数ベクトル$\bm{l}$との間の角度が$\phi_{l}$である。2次元波数ベクトル$\bm{l}$をもつ温度ゆらぎのフーリエモードに対して、$E$モードはそのゆらぎの方向$\bm{l}$に平行、または垂直な方向への偏光成分である。$B$モードは$\bm{l}$から45度傾いた方向への偏光成分である。一般に、直線偏光はこの2つのモードの重ね合わせからなる。

線形密度ゆらぎから生じる偏光は$B$モードを生じない。一般に、線形密度ゆらぎは各フーリエモードの重ね合せで与えられるため、そこから生じる偏光は$E$モードのみである。

$B$モードが生じる原因は、ふたつ考えられる。ひとつ目は、弱い重力レンズ効果である。重力レンズ効果により、光は進行方向を変える。このとき、重力レンズの効果は、最終散乱面のゆらぎとはほとんど関係がないので、1次的な温度ゆらぎから幾何学的に決まる$E/B$モードの区別ができなくなる。ふたつ目は、重力波によるものである。重力波は空間を非等方に歪めるので、光の散乱時に偏光を生じる。重力波による空間の歪みは、重力波の波数ベクトルと垂直な平面の方向になる。このため、密度ゆらぎの場合と異なり$B$モード$E$モードと同様に生成される。

偏光の$E/B$モードの検出は、パワースペクトルを観測量として求められる。$E/B$モードのパワースペクトル$C^{EE}_{l}$、$C^{BB}_l$は次のように定義される。
\begin{align}
 \left<E^{*}(\bm{l})E(\bm{l})\right> = (2\pi)^{2}\delta^{2}(\bm{l} - \bm{l}')C^{EE}_{l} \\
 \left<B^{*}(\bm{l})B(\bm{l})\right> = (2\pi)^{2}\delta^{2}(\bm{l} - \bm{l}')C^{BB}_{l} 
\end{align}

重力波のパワースペクトルへの寄与は、密度ゆらぎによる寄与よりも小さい。重力波の振幅は、ハッブル半径内減衰してしまうので、小角度スケールのパワースペクトルへの寄与は小さくなる。重力波が寄与すると、大角度スケールの温度パワースペクトルだけが強められる。逆に、弱い重力レンズ効果のパワースペクトルへの寄与は、小角度スケールで大きくなりる。つまり、インフレーションによる重力波（原始重力波）を観測するためには、大角度スケールの$B$モードを観測する必要がある。

\section{CMB偏光観測とその現状}

インフレーション理論は、ビッグバン宇宙模型の問題を一挙に解決し、また、現在の宇宙構造の種になる微小なゆらぎをつくれることなど魅力的な理論であるが、インフレーションの具体的な模型は多種多様に存在するため、実際にこの宇宙がどの模型を採用しているのかわかっていない。そこで、実際にこの宇宙を観測することでこれらの模型を制限することが考えらる。特にCMB偏光の精密測定は、インフレーション模型を制限する有力な方法のひとつである。

\subsection{CMB偏光実験の過去、現在、未来}

昨年、2013年3月にBICEP2実験が、原始重力波起源の$B$モードを発見したと発表した\cite{bicep2}。BICEP2実験は、南極

\section{GroundBIRD実験}

GroundBIRD実験は、大角度スケールの原始重力波起源$B$モードの発見とその精密測定を行う地上実験である。図\ref{fig:cl_gb}にGroundBIRD実験が狙うパワースペクトルの領域を示す。

\begin{figure}[htbp]
 \centering
 \includegraphics[width=15cm]{figures/gb.eps}
 \caption{GroundBIRDは望遠鏡自身が高速に回転して、CMB偏光に変調をかける。これにより、$1/f$ノイズの影響を受けない測定が可能となる。}
 \label{fig:gb}
\end{figure}

\begin{figure}[htbp]
 \centering
 \includegraphics[width=15cm]{figures/cl.eps}
 \caption{GroundBIRDが狙う角度スケール}
 \label{fig:cl_gb}
\end{figure}

\subsection{スキャン・ストラテジー}

GroundBIRDは、広い観測領域を観測する（図\ref{fig:scan}）。一般に、観測領域を制限するのは、検出器のベースラインのゆらぎに起因する$1/f$ノイズである。GroundBIRDはこの$1/f$ノイズの影響を受けない領域で観測するために、望遠鏡自身が高速（20 RPM）で回転しながら観測する。

\begin{figure}[htbp]
 \centering
 \includegraphics[width=15cm]{figures/scan_strategy.eps}
 \caption{GroundBIRDの観測する領域。望遠鏡は天頂角に対して20$^{\circ}$傾けた状態で回転させ観測を行う。これにより、全天のおよそ30\%を掃く。地球の自転と望遠鏡の回転により、ドーナッツ型のスキャンになる。}
 \label{fig:scan}
\end{figure}

\subsection{偏光応答の較正}

望遠鏡の回転を利用して、観測を行いながら偏光応答の較正を行う。一定周期で回転する偏光信号は、回転の2倍周期の変調信号となる。この偏光強度をX軸方向のアンテナとY軸方向のアンテナについて比較することにより、ゲインの不揃いを較正する（図\ref{fig:calib}）。

\begin{figure}[htbp]
 \centering
 \includegraphics[width=12cm]{figures/calib.eps}
 \caption{望遠鏡の視野に金属線を平行に張る。望遠鏡が回転することにより、CMBの偏光信号が変調される。}
 \label{fig:calib}
\end{figure}

\subsection{冷却光学系}

\subsection{検出器}

GroundBIRDはCMB観測実験としてはじめて、検出器として超伝導共振器MKID（Microwave Kinetic Inductance Detector）を採用する。検出器としてのMKIDの詳細は、次章で述べる。

% \begin{figure}[htbp]
%  \centering
%  \includegraphics[width=15cm]{figures/calib_wire.eps}
%  \caption{大気のゆらぎのニー周波数}
%  \label{fig:calib_wire}
% \end{figure}
